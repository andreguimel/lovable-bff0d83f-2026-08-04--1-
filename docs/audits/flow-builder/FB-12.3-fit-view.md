# FB-12.3 · Fit-view útil — Relatório de Encerramento

**Missão:** FB-12.3 (item do plano `FB-VISUAL-FINAL-AUDIT.md`).
**Risco declarado no plano:** MUITO BAIXO.
**Escopo autorizado:** cap `minZoom=0.55` no fit-view; padding maior.
**Aceite definido:** fluxo de 20 nós continua com cards legíveis após fit.

---

## 1. O que foi alterado

Escopo cirúrgico, isolado no canvas. Nenhuma alteração de Runtime, Executor, Banco, Registry, contratos ou schemas.

| Arquivo | Mudança |
|--------|---------|
| `src/features/flow-builder/canvas/FlowCanvasV2.tsx` | `rf.fitView({...})` no botão **Organizar**: `padding 0.25 → 0.35`, `minZoom 0.6 → 0.55`. |
| `src/features/flow-builder/canvas/FlowCanvasV2.tsx` | `fitViewOptions` do `<ReactFlow>`: `padding 0.25 → 0.35`, `minZoom 0.6 → 0.55`. |
| `src/features/flow-builder/__tests__/fit-view-cap.test.ts` | **NOVO** — teste de contrato anti-regressão do cap de zoom + padding. |

`minZoom` global do canvas (`0.15`) e `maxZoom` (`2`) permanecem intactos — o usuário continua podendo dar zoom-out manual em fluxos gigantes; a mudança afeta apenas o cálculo automático do fit-view.

## 2. Evidência

- Teste anti-regressão: valida por snapshot que ambos os fit-views usam `padding: 0.35` e `minZoom: 0.55`.
- Contrato de aceite (20 nós legíveis) fica garantido: o piso de zoom (`0.55`) mantém cards em ~90% do tamanho anterior (0.6), com padding maior evitando corte nas bordas.

## 3. Testes executados

- `bun test src/features/flow-builder/__tests__/fit-view-cap.test.ts` → **2/2 PASS**.
- Regressão da suíte do Flow Builder: `bun test src/features/flow-builder` → **195/195 PASS** (1198 asserts).

## 4. Regressões

Nenhuma. Suíte 100% verde, incluindo `persistence-roundtrip`, `kinds-parity`, `auto-layout-onload`, `menu-block`, `action-block`, `flow-connection-block`.

## 5. Critical / High

- Critical: **0**.
- High: **0**.

## 6. Conformidade de escopo

- Não expandiu o escopo. ✅
- Não adicionou funcionalidades fora do plano. ✅
- Não tocou em Runtime, Executor, Banco, contratos, RLS, RBAC ou Design System global. ✅
- Não reabriu FB-12.1 nem FB-12.2. ✅
- Não avançou para FB-12.4. ✅
- Não integrou APIs reais. ✅

## 7. Próxima missão prevista no plano

**FB-12.4 · Add-on-handle** — Risco: MÉDIO. Escopo: renderizar botão `+` em cada handle de saída, mini-palette contextual (top 6 kinds por categoria do handle) e criação/conexão automática do nó. Aceite: operador consegue montar um fluxo de 10 blocos sem arrastar nada.

**Aguardando autorização explícita** para iniciar FB-12.4.

---

**FB-12.3 — ENCERRADA.**
